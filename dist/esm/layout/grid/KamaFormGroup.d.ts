import React, { ReactNode } from 'react';
type KamaFormGroupProps = {
    children?: ReactNode;
};
declare function KamaFormGroup({ children }: KamaFormGroupProps): React.JSX.Element;
export default KamaFormGroup;
